import React, { FC } from 'react';
import Favorites from '../../src/components/favorites';

interface IProps {}

const FavoritesPage: FC<IProps> = () => {
	return <Favorites />;
};

export default FavoritesPage;
